import 'package:flutter/material.dart';

class ForwardText extends StatelessWidget {
  const ForwardText({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
